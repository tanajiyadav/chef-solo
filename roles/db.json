{
  "name": "db",
  "description": "The base role for systems that serve database server.",
  "chef_type": "role",
  "json_class": "Chef::Role",

  "run_list" : [
  	"recipe[common]",
  	"recipe[common::yumrepo]",
  	"recipe[common::set_env]"
  ]
}
